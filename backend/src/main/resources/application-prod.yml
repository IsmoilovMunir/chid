spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/chid_mortgage}
    username: ${DATABASE_USER:chid}
    password: ${DATABASE_PASSWORD:chid}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
