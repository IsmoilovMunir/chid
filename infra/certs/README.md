# Place TLS certificates here for --profile tls:
#   fullchain.pem
#   privkey.pem
#
# Example:
#   SSL_CERTS_DIR=/etc/letsencrypt/live/chid.ru
#
# Or copy/symlink into this directory. For local smoke use --profile local (no certs).
